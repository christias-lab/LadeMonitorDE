# ADR 0006: MapLibre with configurable tile hosting

Status: accepted

Use MapLibre for vendor-neutral vector rendering. The style URL is runtime-configurable. The Phase 1 default is a development/demo style only. Production requires an explicitly licensed hosted provider or self-hosted tiles with correct attribution and capacity planning.

